import 'package:flutter/material.dart';

class ArrendarScreen extends StatelessWidget {
  static const routeName = '/inmuebles/renta/';

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('arrendar'));
  }
}
